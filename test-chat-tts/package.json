{
  "name": "@nimiplatform/mod-test-chat-tts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build-mod.mjs --mod test-chat-tts",
    "watch": "node ../scripts/build-mod.mjs --mod test-chat-tts --watch",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "npx tsx --test test/*.test.ts",
    "verify": "pnpm run typecheck && pnpm run test && pnpm run build",
    "clean": "rm -rf dist"
  }
}
