{
  "name": "@nimiplatform/mod-re-life",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build-mod.mjs --mod re-life",
    "watch": "node ../scripts/build-mod.mjs --mod re-life --watch",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "clean": "rm -rf dist"
  }
}
