{
  "name": "@nimiplatform/mod-kismet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build-mod.mjs --mod kismet",
    "watch": "node ../scripts/build-mod.mjs --mod kismet --watch",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "generate:city-catalog": "node data/generate-city-catalog.mjs",
    "probe:runtime": "node ../node_modules/.pnpm/node_modules/tsx/dist/cli.mjs test/scripts/run-kismet-runtime-probe.ts",
    "test": "node ../node_modules/.pnpm/node_modules/tsx/dist/cli.mjs --test test/*.test.ts",
    "verify": "pnpm run typecheck && pnpm run test && pnpm run build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "lightweight-charts": "^5.1.0"
  }
}
