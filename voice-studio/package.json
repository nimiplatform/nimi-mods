{
  "name": "@nimiplatform/mod-voice-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build-mod.mjs --mod voice-studio",
    "watch": "node ../scripts/build-mod.mjs --mod voice-studio --watch",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
