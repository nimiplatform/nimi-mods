{
  "name": "@nimiplatform/mod-knowledge-base",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build-mod.mjs --mod knowledge-base",
    "watch": "node ../scripts/build-mod.mjs --mod knowledge-base --watch",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.6",
    "@radix-ui/react-progress": "^1.1.2",
    "@radix-ui/react-tooltip": "^1.1.8"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
